import React from 'react';
import './CallToAction.css'; // This assumes the CSS file is in the same folder as the component

const CallToAction = () => {
  return (
    <div className="call-to-action">
      {/* Your component code here */}
    </div>
  );
};

export default CallToAction;
